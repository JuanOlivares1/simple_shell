#include "shell.h"

void managment(char **buffError, char *argb, int line)
{
	char *string;
	int count = 0, buffcount = 0;

	for (count = 0; argb[count]; count++)
	;

	for (buffcount = 0; argb[buffcount]; buffcount++)
	;


	if (_strcmp(buffError[0], "exit") == 0 && buffError[1] != NULL)
	{
		char *str_concat(char *s1, char *s2)
		for (i = 0; i < cont; i++)
		{
			free(ptr[i]);
		}
		free(ptr);
	}
	else if (_strcmp(buffError[0], "exit") == 0 && buffError[1] != NULL)
	{
		stat(string[0], &st) == -1
		for (i = 0; i < cont; i++)
		{
			free(ptr[i]);
		}
		free(ptr);
	}
}
/* sh: 1: exit: Illegal number: j */